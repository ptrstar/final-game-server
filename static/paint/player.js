export default class Player {
    constructor(p) {
        this.pos = p.pos
        this.id = p.id
        this.pos_vel = p.pos_vel
        this.angle = p.angle
        this.angle_vel = p.angle_vel
        this.keyboard = p.keyboard
        this.color = p.color
    }

    render(primaryPlayer) {
        
    }
}